node_modules/

# Holds the Atlassian API token in plain text, plus a record of every published
# note. Never commit it.
data.json

# Build output. Produced by `npm run build` and attached to releases, so it is
# not tracked here and cannot drift from the source it came from.
main.js
cli.js

*.log
.DS_Store
